// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.google.guava:guava:22.0'
        classpath 'com.android.tools.build:gradle:3.1.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

// I am trying to implement the optional task that Launches the GCE local development server,
// Runs all tests, and Shuts the server down again
// I am not sure if this is the right way please give me feedback on how to do it right
// To tie it all together, create a Gradle task that:

task integerationTest(type: GradleBuild) {
    tasks = ['appengineStart', 'test', 'appengineStop']
}